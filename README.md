# Star Wars Character App

A simple React app to browse Star Wars characters using SWAPI.

## What It Does
- Shows list of characters with name + image
- Click card → opens modal with details (height, mass, films, homeworld, etc.)
- Search by name (with delay so it doesn't search while typing)
- Filter by species, planet, or film
- Login with `admin` / `password` (fake login)
- Works on phone, tablet, desktop
- Has loading and error messages

## How to Run
```bash
git clone https://github.com/himanshu474/star-wars-character-app.git
cd star-wars-character-app
npm install
npm start